collection Person {
  properties {
    name []str
      @maxLength(2)
      @minLength(10)

    age num @minimum(50)
    options {
      properties {
        disabled num
        picture File @accept(12)
        friends []User @indexes(["name"])
      }
    }
  }
}


