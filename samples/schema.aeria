collection Person {
  required {
    name
  }

  properties {
    name str
    age int @maxLength(10)
    picture File @accept(["image/*"])
  }
}

collection Pet {
  properties {
    name str
    specie enum @options([
      "dog",
      "cat"
    ])
  }
}

